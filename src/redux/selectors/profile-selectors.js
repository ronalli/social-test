export const getProfileSelector = (state) => {
	return state.profilePage.profile;
}

export const getProfileUserIdSelector = (state) => {
	return state.profilePage.profileUserId;
}

export const getStatusSelector = (state) => {
	return state.profilePage.status;
}

export const getIdSelector = (state) => {
	return state.profilePage.id;
}